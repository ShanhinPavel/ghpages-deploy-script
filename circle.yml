test:
  override:
    - exit 0 # pretend we did some tests?
general:
  branches:
    ignore:
      - gh-pages # don't do anything if we deploy to gh-pages
deployment:
  staging:
    branch: master
    commands:
      # deploy build directory to gh-pages
      - ./scripts/deploy_staging.sh build
