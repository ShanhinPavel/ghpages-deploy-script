general:
  #switch to whitelisting as blacklisting doesn't seem to work!
  branches:
    only:
      - master

test:
  override:
    - exit 0 # pretend we did some tests?

deployment:
  staging:
    branch: master
    commands:
      # deploy build directory to gh-pages
      - ./scripts/deploy-ghpages.sh build
